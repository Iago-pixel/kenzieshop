import styled from "styled-components";

export const Container = styled.main`
  > h1 {
    font-weight: lighter;
    color: var(--orange);
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }
  .paddingBottom {
    height: 100px;
  }
`;
